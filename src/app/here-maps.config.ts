import { environment } from './../environments/environment.development';

export const hereMapsConfig = {
    apiKey: environment.apiKey, // Replace with your actual Here Maps API key
    appId: environment.appId,   // Replace with your Here Maps App ID
  };